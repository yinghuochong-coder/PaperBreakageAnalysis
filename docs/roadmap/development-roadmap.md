# 纸机断纸分析边缘系统开发路线图

## 1. 文档信息

| 项目 | 内容 |
| --- | --- |
| 状态 | 待执行 |
| 计划基线 | 2026-07-31 |
| 需求来源 | `docs/requirements/edge-system-requirements.md` |
| 适用范围 | 首期边缘工控机软件，Windows 10/11 x64，最多四台 MV-CS020-60GM |
| 目标工具链 | Visual Studio 2026 MSVC、C++20、Qt 6、CMake |
| 执行方式 | 一次只领取一个可审查任务；复杂任务先按 `.agent/PLANS.md` 建立 ExecPlan |

本文档是实施顺序、任务边界和验收门禁的基线，不代替需求文档。需求变更后，应先更新需求与追踪矩阵，再调整本路线图。

## 2. 当前基线与计划假设

### 2.1 仓库现状

截至计划基线日期，仓库中只有：

- `AGENTS.md`；
- `docs/requirements/edge-system-requirements.md`。

当前不存在源码、CMake 工程、测试、CI、架构文档或既有路线图；当前目录也不是 Git 仓库。因此所有里程碑均按“从空工程开始”规划。

### 2.2 必须先解决的不一致与未知项

以下事项不得在实现中静默猜测：

| 编号 | 事项 | 当前处理原则 | 最晚决策点 |
| --- | --- | --- | --- |
| DEC-001 | VS 2026、MSVC 和 CMake 最低版本未形成一致工具链 | 已接受：VS 2026 stable、MSVC v145、CMake 最低 4.2；见 ADR-015 | G0-03 |
| DEC-002 | Qt 路径 `C:\Qt\6.10.2\msvc2022\_64` 不是实际目录名且不应固化 | 已接受：Qt 6.10.2 `msvc2022_64`，根目录只由环境/用户预设注入 | G0-03 |
| DEC-003 | OpenCV、MVS SDK 的具体版本未记录 | 已接受：OpenCV 4.12.0、MVS SDK 4.8.0.3；MVS 仅在 Hikrobot 适配器启用 | G0-03 |
| DEC-004 | 已由 ADR-012 决定 | Uplink v1 固定为明文 HTTP/WS、无鉴权，并以参考模拟器冻结端点、消息和断点上传契约 | M8-00 |
| DEC-005 | PLC/现场 IO 协议未确定 | 首期只定义接口和 Mock；实际协议不进入首期完成门禁 | M8-00 |
| DEC-006 | 正式断纸算法、阈值和数据集未给定 | M5 先跑通模拟事件链，M6 单独评审算法验收数据集和指标 | M6-00 |
| DEC-007 | 工控机 CPU、内存、NVMe 容量和相机实际 ROI 未给定 | 队列和缓存必须有显式上限；最终容量以性能测量校准 | M2-03 / M7-01 |
| DEC-008 | 安装器技术选型未确定 | M9 前做 ADR，比较 WiX、MSIX 或批准的企业部署方式 | M9-01 |

### 2.3 硬件与外部依赖假设

- 日常自动化测试使用模拟相机，不依赖实体相机。
- M3 和 G9 的硬件条目必须在安装了 MVS SDK 的目标 Windows 工控机上执行。
- 四路真机、拔网线恢复、磁盘压力、异常断电恢复和 7×24 小时稳定性测试必须保留原始记录；未执行时只能标记“待硬件验证”。
- 生产证书、Token、PLC 和上位机凭据不进入源码库。

## 3. 首期范围和完成定义

### 3.1 首期必须交付

1. Windows 服务可自动启动，未登录桌面时仍可采集。
2. Qt 客户端可独立打开、关闭和自动重连，不影响后台服务。
3. 最多四路相机可配置、绑定、采集、统计和掉线恢复。
4. 预览以 2～5 fps 独立抽样，不能反压高速采集。
5. 模拟或人工触发候选事件后，能冻结并保存配置的前后图像。
6. 事件具备清单、关键帧、校验值、SQLite 索引和崩溃恢复语义。
7. 上位机离线时本地工作不受影响，恢复后可补传。
8. 磁盘水位、服务异常、相机异常和关键队列积压可监测并报警。
9. 在目标工控机上完成四路真机和 7×24 小时稳定性验收。

### 3.2 首期明确不做

- 复杂多机深度学习融合、云端训练、自动训练；
- 完整上位机业务系统；
- 长时间全量原图中心存储或自定义视频编解码器；
- 多用户复杂权限系统；
- Qt 直接控制 PLC 安全停机；
- 工控机之间协同；
- 无人工确认的自动根因结论；
- 在协议未批准前实现具体 PLC 生产适配器。

## 4. 执行与验收规则

### 4.1 任务状态

每个任务只能处于以下状态之一：

- `not-started`：尚未开始；
- `in-progress`：已有负责人和执行计划；
- `blocked`：存在明确外部阻塞，已记录解除条件；
- `completed`：满足任务验收且证据已归档。

路线图默认所有任务为 `not-started`。开始任务时，在对应任务标题后标记状态、负责人、开始日期和 ExecPlan 路径。

### 4.2 单任务完成清单

每个行为或架构变更在标记完成前必须同时满足：

- 只修改任务范围内文件；
- 新增或更新相应测试；
- Debug 构建成功；
- 与发布有关的任务还需 Release 构建成功；
- 相关 CTest 通过；
- 编译警告按错误处理；
- 新增跨线程队列已记录容量、溢出策略和关闭行为；
- 新增线程具有停止令牌、最长等待时间和确定性 join 路径；
- 新增外部输入具有类型、范围和依赖校验；
- 公开行为、文件格式或模块边界变更已同步文档；
- 最终记录执行命令、结果、未执行项和限制；
- 涉及硬件时明确区分模拟验证与真机验证。

### 4.3 推荐验证命令

工程骨架建立后，常规任务至少运行：

```powershell
cmake --preset windows-vs2026-debug
cmake --build --preset windows-vs2026-debug
ctest --preset windows-vs2026-debug
```

发布或部署任务还需运行：

```powershell
cmake --preset windows-vs2026-release
cmake --build --preset windows-vs2026-release
ctest --preset windows-vs2026-release
```

具体任务可以增加筛选测试、静态分析、故障注入和性能测试，但不得用较小测试集替代该任务约定的验收集。

### 4.4 阶段门禁

```text
G0 规划与架构基线
 ↓
M0 工程骨架
 ↓
M1 服务与 IPC
 ↓
M2 相机抽象与模拟器
 ├──────────────┐
 ↓              ↓
M3 真机适配     M4 客户端功能（先用模拟相机）
 └──────┬───────┘
        ↓
M5 内存缓存与完整事件链
        ↓
M6 正式算法接口与初版算法
        ↓
M7 NVMe 滚动缓存
        ↓
M8 上位机通信
        ↓
M9 部署与系统验收
```

M3 和 M4 可在 M2 完成后并行，但进入 M5 系统联调前必须分别通过各自阶段门禁。

## 5. G0：规划、架构与决策基线

目标：在修改模块边界前形成可审查的架构基线，消除会影响工程骨架的关键歧义。

### G0-01 建立系统架构文档

状态：`completed`  
完成日期：2026-07-31  
执行记录：`.agent/plans/g0-01-system-architecture.md`

前置：本路线图已批准。

实施：

- 创建 `docs/architecture/system-architecture.md`；
- 明确两个进程、库目标、依赖方向和禁止依赖；
- 定义服务核心与 Windows SCM 宿主的隔离边界；
- 定义 `camera/interfaces`、`camera/mock`、`camera/hikrobot` 边界；
- 定义采集、处理、缓存、事件、预览、IPC、上传的数据流；
- 为每条跨线程通道记录生产者、消费者、容量配置、溢出策略和关闭顺序；
- 记录 MVS SDK 只允许存在于 Hikrobot 适配器；
- 记录 Windows 专用代码隔离位置和可测试接口；
- 为关键架构选择建立 ADR 索引。

产物：系统上下文图、组件图、线程/数据流图、依赖规则、关闭时序。

验证：文档审查可追溯到需求第 3、7、8、9、16 节；不存在业务模块直接依赖 MVS SDK 的设计。

### G0-02 固化术语、业务错误和时间语义

状态：`completed`

完成日期：2026-07-31

执行记录：`.agent/plans/g0-02-domain-errors-time.md`

实施：

- 定义 Camera ID、事件、候选、确认、拒绝、超时、上传任务等术语；
- 区分单调时钟和墙上时钟用途；
- 定义全局事件 ID 的唯一性和时区格式；
- 建立稳定业务错误码登记表，同时保留厂商码和上下文；
- 定义配置版本、协议版本、事件 schema 版本和数据库 schema 版本的独立演进规则。

产物：`docs/architecture/domain-model.md`、`docs/error-codes.md`。

验证：示例可覆盖配置错误、相机错误、队列溢出、事件写入、数据库、IPC 和上传失败。

### G0-03 记录依赖和工具链决策

状态：`completed`

完成日期：2026-07-31

执行记录：`.agent/plans/g0-03-dependencies-toolchain.md`

实施：

- 确认 VS 2026、CMake 最低版本、Qt 版本与组件；
- 确认依赖获取策略，记录 Qt、OpenCV、MVS SDK 为外部 SDK，其他批准依赖可由 vcpkg 管理；
- 为 spdlog、nlohmann/json、GoogleTest、SQLite3 和可选 zstd 记录引入原因、版本和许可证；
- 禁止把开发机绝对路径固化到提交的 Release 配置；
- 确认 CI 可用的 Windows 执行器。

产物：`docs/architecture/dependencies.md`、相关 ADR。

退出门禁 G0：

- 架构、域模型和依赖决策完成评审；
- DEC-001～DEC-003 有结论；
- 后续里程碑的模块边界不再依赖未决假设。

## 6. M0：工程骨架

- 状态：`completed`
- 负责人：Codex
- 开始日期：2026-08-01
- 完成日期：2026-08-01
- 执行记录：`.agent/plans/m0-engineering-skeleton.md`

目标：建立可重复构建、测试和运行的最小工程，不实现业务功能。

### M0-01 建立 CMake 工程和预设

实施：

- 创建顶层 `CMakeLists.txt` 和模块目录；
- 创建 Debug、Release、测试预设；
- 仅支持 Windows x64、MSVC 和 C++20；
- 启用 UTF-8、严格警告、警告视为错误；
- 将本机 SDK 路径留给 `CMakeUserPresets.json` 或环境配置；
- 生成版本号、提交标识（若仓库已初始化 Git）、构建时间和依赖版本；
- 提供安装布局但暂不制作安装器。

测试：

- 配置 Debug 和 Release；
- 验证错误架构、缺失必需依赖时给出明确错误；
- 验证生成产物中没有开发机绝对依赖路径。

### M0-02 建立目标和目录边界

创建最小目标：

- `paperbreak_common`；
- `paperbreak_camera`；
- `paperbreak_pipeline`；
- `paperbreak_algorithm`；
- `paperbreak_event`；
- `paperbreak_storage`；
- `paperbreak_uplink`；
- `PaperBreakEdgeService`；
- `PaperBreakEdgeConsole`；
- `PaperBreakTests`。

每个目标只放占位入口和清晰依赖，不提前实现后续里程碑。

### M0-03 日志、错误结果和版本信息基础

实施：

- 封装异步日志初始化、模块分类、按大小/日期滚动和脱敏规则；
- 定义业务 `Error`/`Result` 表达，不把厂商码作为唯一错误；
- 服务和控制台输出统一版本信息；
- 确保日志后台线程可刷新并确定性停止。

测试：

- 日志轮转、等级过滤、敏感字段脱敏；
- 业务码、原始码和上下文能共同保留；
- 日志目录不可写时返回可诊断错误。

### M0-04 测试与 CI 基线

实施：

- 集成 GoogleTest 和 CTest；
- 建立 unit、integration、simulation、hardware-integration 标签；
- 默认 CI 排除 hardware-integration；
- 配置格式检查、静态分析和测试报告；
- 提供一个公共库测试、服务控制台冒烟测试和 Qt 客户端启动冒烟测试。

退出门禁 M0：

- Debug/Release 均能配置和构建；
- 服务以 `--console` 启动后可受控退出；
- Qt 客户端能启动并显示最小托盘；
- CTest 能运行，CI 脚本不依赖实体相机；
- 依赖与构建说明已写入开发文档。

## 7. M1：Windows 服务、配置基础与 IPC

目标：完成独立后台进程、可靠配置和本机控制通道。

### M1-01 服务核心生命周期

状态：`completed`
负责人：Codex
开始日期：2026-08-01
完成日期：2026-08-01
执行记录：`.agent/plans/m1-01-service-core-lifecycle.md`

实施：

- 将可测试的 `ServiceRuntime` 与 SCM 宿主分离；
- 实现 `--console`、`--validate-config`；
- 实现启动、停止、系统关机处理和有界停止超时；
- 按规定顺序停止接收配置、采集/处理、事件、上传、IPC 和日志；
- 启动失败返回稳定业务错误并记录阶段。

测试：重复启停、启动中取消、部分组件启动失败回滚、停止超时、异常不穿越 Win32 回调。

### M1-02 Windows SCM 集成

状态：`completed`

负责人：Codex

开始日期：2026-08-01

完成日期：2026-08-01

执行记录：`.agent/plans/m1-02-windows-scm-integration.md`

实施：

- 实现 `--install`、`--uninstall`；
- 上报 START_PENDING、RUNNING、STOP_PENDING、STOPPED；
- 配置自动启动和异常恢复策略；
- 服务进程不创建界面；
- 安装/卸载要求管理员权限并具备幂等结果。

验证：在隔离 Windows 测试机执行安装、重启、未登录启动、停止和恢复测试；自动化单测只覆盖封装层。【请忽略 系统重启 之类的验证】

### M1-03 配置模型、校验和原子存储

状态：`completed`

完成证据（2026-08-01）：提交 schema v1、完整默认配置、强类型/依赖/路径/敏感字段校验、不可变快照、期望修订事务、Windows 原子替换、5 份历史恢复/回滚、热应用/待重启分离和同步脱敏审计。`cmake --build --preset local-windows-vs2026-debug` 通过；`ctest --preset local-windows-vs2026-debug` 为 17/17，通过的 unit 入口包含 40 项测试（其中配置仓储 7 项），排除 `hardware-integration`。未访问实体相机或 MVS SDK。

实施：

- 建立版本化 JSON schema 和强类型配置模型；
- 覆盖系统、相机、采集、预览、算法、事件、存储、上位机、PLC/IO、日志、健康参数；
- 校验类型、范围、跨字段依赖和路径；
- 采用同目录临时文件、flush 和原子替换；
- 保留有限历史版本，支持失败回滚；
- 区分立即生效与重启生效；
- 记录修改来源、版本、时间和脱敏审计数据；
- 不在普通 JSON 保存密码、Token 和私钥。

测试：合法/非法边界、中文和空格路径、截断文件、临时文件残留、版本冲突、写入失败、应用失败回滚。

### M1-04 IPC 帧协议和服务端

状态：`completed`

实现证据（2026-08-01）：IPC v1、Windows 本机身份鉴权、单实例保护、三条 system 命令及 ServiceRuntime 接入已实现；配置解析已按顶层分区拆分，既有全仓格式违规和 C6262 栈使用告警均已清除。Debug/Release 构建、全仓 `format-check` 和默认 `/WX` 静态分析通过，两套非硬件 CTest 均为 17/17，通过的 unit 入口包含 72 项测试。跨账户、提升管理员及远程命名管道场景仍待隔离 Windows 环境验证；未访问相机或 MVS SDK。

实施：

- 基于 QLocalServer/QLocalSocket 实现仅本机 IPC；
- 使用固定字节序长度前缀、JSON 头和可选二进制负载；
- 设置单消息上限、连接数上限、读取超时和积压上限；
- 实现协议版本、requestId、请求/响应/推送关联；
- 先实现 `system.getStatus`、`system.getVersion`、`system.reloadConfig`；
- 对畸形长度、无效 JSON、未知命令和协议不兼容返回稳定错误；
- 配置本机访问权限。

测试：拆包/粘包、零长度、超大长度、随机字节、慢客户端、重复 requestId、断线期间推送、服务停止。

### M1-05 Qt IPC 客户端与重连

状态：`completed`

负责人：Codex

开始日期：2026-08-01

执行记录：`.agent/plans/m1-05-qt-ipc-client-reconnect.md`

实现证据（2026-08-01）：已实现客户端方向 IPC v1 编解码、有界请求、连接代次、超时/取消和带抖动上限的自动重连；Console 状态模型会在每代连接后查询服务状态，断线明确标记过期，退出只关闭客户端 IPC。新增协议、客户端和状态模型定向测试 16 项通过，重复 3 轮共 48 次通过；既有全仓格式违规和 C6262 栈使用告警已清除，Debug/Release 构建、全仓 `format-check`、默认 `/WX` 静态分析及两套非硬件 CTest 均通过，CTest 为 17/17（单元测试入口 72 项）。

实施：

- 提供连接状态模型；
- 使用带抖动的有上限退避重连；
- 请求具备超时、取消和连接代次，旧响应不能污染新连接；
- 客户端断开时明确清除或标记状态为过期；
- 客户端退出只关闭 IPC，不停止服务。

测试：服务先启动/后启动、服务重启、客户端反复开关、请求中断、过期响应。

### M1-06 健康、报警和近期日志基础

状态：`completed`

负责人：Codex

开始/完成日期：2026-08-01

执行记录：`.agent/plans/m1-06-health-alarm-log.md`

实现证据（2026-08-01）：新增 `paperbreak_monitoring`，完成 1024 项/64 来源指标注册表、1024 活动/4096 历史报警登记表、Windows/进程/四类目录卷采样、IPC 指标、默认 2048 条近期日志环、健康线程热更新与确定性关闭，以及四条 IPC 命令和四类状态/报警推送。数据库在 M5 前明确报告 `not-initialized`。Debug/Release `/WX` 构建、两套非硬件 CTest、全仓 `format-check`、默认 MSVC 静态分析和 `git diff --check` 均通过；两套 CTest 均为 17/17，unit 入口 87 项。未执行实体相机、MVS SDK、SCM 重启或目标机性能测试。

实施：

- 建立线程安全的指标快照、报警登记表和状态推送接口；
- 报警包含 code、severity、source、首次/最近时间、active、次数、消息、details 和 acknowledged；
- 支持报警 raise、重复合并、clear 和 acknowledge；
- 提供服务运行时间、CPU、内存、线程、句柄、磁盘、数据库和 IPC 基础指标；
- 实现 `system.getMetrics`、`alarm.list`、`alarm.acknowledge`、`log.tail`；
- 采样和日志查询使用有界结果，慢客户端不得阻塞监测生产者；
- 后续阶段只注册各自相机、算法、存储和上位机指标/报警，不另建平行状态源。

测试：重复报警计数、清除后再发生、确认权限、并发读写、历史结果上限、服务停止时监测线程退出。

退出门禁 M1：

- 界面退出后服务继续运行；
- 服务重启后界面自动重连；
- 非法 IPC 输入和慢客户端不会拖垮服务；
- 配置可验证、原子保存、回滚和审计；
- SCM 真机验证结果已归档，或明确标记待目标机验证。

## 8. M2：相机抽象、模拟器与采集管线

目标：在无厂商 SDK 和实体相机条件下跑通四路采集、状态、队列和故障恢复。

### M2-01 相机接口和领域模型

状态：`completed`

负责人：Codex

开始日期：2026-08-01

完成日期：2026-08-01

执行记录：`.agent/plans/m2-01-camera-interface-domain-model.md`

完成证据（2026-08-01）：已实现厂商无关 `ICameraProvider`/`ICameraDevice`、能力与参数快照、序列号绑定和参数校验、固定容量 `FrameBuffer`、完整帧元数据与零拷贝只读 `FrameView`；新增 9 项相机单元测试。Debug/Release、格式检查和 MSVC 静态分析构建通过，非硬件 CTest 17/17 通过，unit 入口共 96 项测试。未访问 MVS SDK 或实体相机，硬件验证留待 M3。

实施：

- 定义 `ICameraProvider`、`ICameraDevice`、能力描述、参数快照和业务错误；
- 定义 `FramePacket`、`FrameBuffer`、`FrameView`、PixelFormat；
- 帧同时携带相机帧号、服务序号、单调时间和墙上时间；
- 禁止业务接口暴露 MVS 类型、句柄和错误码。

测试：不同能力、参数不支持、设备缺失和重复序列号。

### M2-02 相机状态机与重连控制器

状态：`completed`

负责人：Codex

开始日期：2026-08-01

完成日期：2026-08-01

执行记录：`.agent/plans/m2-02-camera-state-reconnect.md`

完成证据（2026-08-01）：已实现八状态完整转换表、结构化转换观察、稳定非法转换错误、默认六次的 1/2/5/10/30/60 秒退避、可取消等待、恢复耗尽和四路隔离；新增 13 项单元测试。Debug/Release、格式检查和 MSVC 静态分析构建通过，非硬件 CTest 17/17 通过，unit 入口共 109 项测试。未访问 MVS SDK 或实体相机，硬件拔线恢复留待 M3。

实现 Disabled、Disconnected、Connecting、Connected、Starting、Streaming、Recovering、Faulted；非法转换必须被拒绝并记录。

测试：

- 全状态转换表；
- 1、2、5、10、30、60 秒退避和成功后重置；
- 停止期间取消重连；
- 最大恢复次数后进入 Faulted；
- 多相机状态互不影响。

### M2-03 固定容量帧池和有界队列

状态：`completed`

负责人：Codex

开始日期：2026-08-01

完成日期：2026-08-01

执行记录：`.agent/plans/m2-03-frame-pool-bounded-queue.md`

完成证据（2026-08-01）：已实现每相机固定容量 `FrameBufferPool`、共享状态安全归还租约、采用 `drop-oldest` 的预分配环形 `AcquisitionQueue`、容量/深度/高水位/背压快照，以及只执行限时取帧、元数据填充和非阻塞入队的可停止 `AcquisitionWorker`；新增 11 项单元测试。Debug/Release、格式检查和 MSVC 静态分析构建通过，非硬件 CTest 17/17 通过，unit 入口共 120 项测试。未访问 MVS SDK 或实体相机，真实 SDK 取流取消和硬件关闭时延留待 M3。

实施：

- 预分配固定容量 FrameBuffer 对象池；
- 采集队列显式配置容量；
- 定义 `drop-newest`、`drop-oldest` 或跳帧策略及指标；
- 获取帧、填元数据、入队是采集路径的唯一职责；
- 队列等待可取消、有超时，关闭后生产者和消费者都能退出；
- 禁止采集路径做磁盘、网络、JPEG 或推理。

测试：池耗尽、队列满、消费者停止、生产者停止、并发关闭、无持续分配增长。

### M2-04 模拟相机和故障注入

状态：`completed`

负责人：Codex

开始日期：2026-08-01

完成日期：2026-08-01

执行记录：`.agent/plans/m2-04-mock-camera-fault-injection.md`

完成证据（2026-08-01）：已新增独立 `paperbreak_camera_mock` 目标、1～4 路严格配置校验、三种确定性图案、四种像素格式、连续/软触发/模拟硬触发、固定容量测试控制通道及七类脚本化/运行期故障；四路模拟相机已接入现有帧池、采集工作线程和有界队列验证故障隔离及掉线重连。新增 9 项单元测试和 1 项模拟集成测试；Debug/Release 非硬件 CTest 17/17、格式检查、20 轮重复测试和 MSVC 静态分析均通过。未访问 MVS SDK 或实体相机，未实现 M2-05。

实施：

- 支持 1～4 路、分辨率、帧率、像素格式和图案配置；
- 支持连续、软触发和模拟硬触发；
- 注入掉线、超时、丢帧、不完整帧、帧号跳变、尺寸/像素格式变化；
- 采用确定性随机种子，测试可重现；
- 提供命令行或测试 API 控制故障。

### M2-05 采集统计与处理骨架

状态：`completed`

负责人：Codex

开始日期：2026-08-01

完成日期：2026-08-01

执行记录：`.agent/plans/m2-05-acquisition-statistics-processing-skeleton.md`

完成证据（2026-08-01）：已实现采集帧率、带宽、帧号缺口、超时、不完整帧、最后帧和队列状态的原子快照；新增透传、有效性检查、四种像素格式灰度统计节点、每相机保序处理执行器、固定容量 `drop-oldest` 算法队列及最多四路统一运行时。新增 10 项管线单元测试、1 项采集统计测试和 1 项四路处理模拟测试；Debug/Release 非硬件 CTest 17/17、格式检查、20 轮重复测试和 MSVC 静态分析均通过，unit 入口共 140 项测试。未访问 MVS SDK 或实体相机，真实像素布局、带宽和硬件关闭时延留待 M3。

实施：

- 统计实际帧率、丢帧、超时、带宽、最后帧时间和队列深度；
- 创建可配置预处理节点链，首批只做透传、灰度统计、有效性检查；
- 处理线程池可停止，算法积压时按已记录策略跳帧；
- 指标读取不能阻塞采集。

退出门禁 M2：

- 四路模拟相机可稳定运行；
- 能复现并验证掉线、超时、丢帧和重连；
- 所有队列都有容量、溢出指标和停止测试；
- 自动化测试不需要 MVS SDK 或真实相机。

## 9. M3：Hikrobot MVS 真机适配

目标：把 MVS SDK 严格封装在适配器内，完成目标相机发现、配置、取流与恢复。

### M3-01 SDK 构建隔离和 RAII 封装

状态：`completed`

负责人：Codex

开始日期：2026-08-01

完成日期：2026-08-01

执行记录：`.agent/plans/m3-01-sdk-build-isolation-raii.md`

完成证据（2026-08-01）：已将 MVS Development/Runtime 查找、4.8.0.3 文件版本校验、x64 include/lib/dll、安装部署和 SDK API 限制在独立 `paperbreak_camera_hikrobot` 目标；实现设备信息深拷贝、设备句柄与取流会话 RAII、稳定业务错误/native code 翻译及捕获标准/未知异常的 `noexcept` C 回调边界。使用本机 MVS SDK/Runtime 4.8.0.3 的 Debug/Release 非硬件 CTest 19/19 通过，其中适配层专用入口含 11 项伪 API/版本 smoke 测试；格式、SDK 边界扫描、安装树路径泄漏扫描、缺 SDK 预期配置失败和 MSVC 静态分析通过。未枚举、连接或取流实体相机，未执行驱动、目标机、带宽和拔线验证，且未开始 M3-02。

实施：

- 所有生产配置固定构建 Hikrobot 适配器；
- 缺少或版本不匹配的 SDK 时在配置阶段失败；
- 将 SDK include、lib、dll 和错误翻译限制在 `camera/hikrobot`；
- 为设备列表、设备句柄和取流生命周期建立 RAII 包装；
- 捕获所有回调异常，禁止异常越过 C API 边界；
- 文档记录批准的 SDK 版本、运行时部署和许可证。

### M3-02 发现、序列号绑定和占用检测

状态：`completed`

负责人：Codex

开始日期：2026-08-01

完成日期：2026-08-01

执行记录：`.agent/plans/m3-02-discovery-binding-occupancy.md`

完成证据（2026-08-01）：新增厂商无关的最多四槽 `CAM01`～`CAM04` 序列号核对报告，可同时呈现 ready、missing、occupied 和未配置设备，并拒绝非法/重复逻辑 ID、配置序列号及发现序列号；Hikrobot Provider 仅调用 MVS GigE 枚举，安全映射型号、序列号、当前 IP、主机网卡 IP，以独占可访问性探测占用，并按精确序列号创建设备。使用本机 MVS SDK/Runtime 4.8.0.3 的 Debug/Release 非硬件 CTest 均为 19/19，其中适配器专用入口 14 项，通用 unit 入口 142 项；格式、SDK 边界、安装路径泄漏、`git diff --check` 和静态分析通过。未访问实体相机，因此真实型号字段、驱动占用竞争、多网卡映射和真机连接仍未验证；未开始 M3-03。

实施：

- 枚举 GigE 相机并映射型号、序列号、IP、网卡；
- 只允许最多四个逻辑槽位；
- 检测重复序列号、配置设备缺失、错误设备占槽和被其他程序占用；
- 业务层只使用 CAM01～CAM04。

### M3-03 参数读写与回读

状态：已完成

开始日期：2026-08-01

完成日期：2026-08-01

执行记录：`.agent/plans/m3-03-parameter-read-write-readback.md`

完成证据（2026-08-01）：Hikrobot 适配器通过私有可注入 API 表映射曝光、增益、帧率、ROI、像素格式、触发模式/源/延迟、GigE 包大小、传输延迟及可证明支持的数字 IO；MVS 浮点节点以连续范围表达，不虚构步长。参数事务在服务/设备双重校验后保存旧快照，必要时暂停取流，按确定顺序写入并完整回读，恢复原取流状态后返回实际值；写入、回读或恢复失败时恢复并确认旧快照，无法恢复则进入 `CAMERA_PARAMETER_FAULTED` 并锁定当前连接会话。使用本机 MVS SDK/Runtime 4.8.0.3 的 Debug/Release 非硬件 CTest 均为 19/19，其中适配器专用入口 21 项、通用 unit 入口 146 项；静态分析、格式、SDK 边界、安装路径泄漏、缺 SDK 负向配置和 `git diff --check` 通过。未访问实体相机，因此 MV-CS020-60GM 的真实节点支持、上下限、传输延迟单位、写后量化、数字 IO 电平及采集中实际回读仍未验证；未开始 M3-04。

覆盖曝光、增益、帧率、ROI、像素格式、触发模式/源/延迟、包大小、传输延迟和支持的数字 IO。

应用流程必须是：服务校验 → 必要时暂停 → 写入 → 回读 → 恢复采集 → 返回实际值。失败时恢复旧快照或进入明确故障状态。

测试：参数能力映射和错误翻译可用伪 SDK/封装测试；真实参数上下限和回读在目标相机执行。

### M3-04 取流和自动恢复

状态：`completed`

负责人：Codex

开始日期：2026-08-01

完成日期：2026-08-01

执行记录：`.agent/plans/m3-04-streaming-automatic-recovery.md`

完成证据（2026-08-01）：Hikrobot 适配器使用 `MV_CC_GetOneFrameTimeout` 把图像直接写入固定池缓冲，映射厂商帧号、经 `GevTimestampTickFrequency` 验证的设备时间戳、丢包不完整标记、宽高/扩展宽高、stride 及 Mono8/Mono10/Mono12/BayerRG8；超时和断流保留稳定业务码、MVS 原始码及有界上下文。公共采集线程检测连续超时阈值和运行中尺寸/格式变化；新增厂商无关恢复会话，串联 Provider 重建、M2 状态机与 1/2/5/10/30/60 秒退避，成功复位、上限 Faulted、停止可取消，并保持固定帧池和采集队列的容量、丢最旧与关闭语义。本机 MVS 4.8.0.3 Debug/Release 非硬件 CTest 均为 19/19，通用 unit 入口 148 项、适配器入口 24 项；格式、静态分析、SDK 边界、安装路径泄漏、缺 SDK 负向配置和 `git diff --check` 通过。未访问实体相机，真实设备时间基准、驱动断线码、拔线恢复、目标帧率/带宽和四路资源趋势仍未验证，保留给 M3-05 硬件门禁；未开始 M3-05。

实施：

- 映射厂商帧号、时间戳、不完整标记和像素元数据；
- 采用预分配缓冲和最小回调路径；
- 检测超时、断流、尺寸和格式变化；
- 与 M2 状态机/退避控制器集成；
- 记录业务码、厂商原始码和上下文。

### M3-05 四路硬件测试工具与记录

状态：`completed`

负责人：Codex

开始日期：2026-08-01

完成日期：2026-08-01

执行记录：`.agent/plans/m3-05-four-camera-hardware-validation.md`

完成证据（2026-08-01）：已提供仅在 Hikrobot 开关开启时构建的 Windows/MSVC 真机验证器、版本化计划样例、不可覆盖的原子 JSON 审计记录及人工故障场景模板；工具通过公共相机接口执行只读发现、目标型号/序列号绑定、参数写入回读、软件触发、1→4 路限时采集及帧率/帧隙/超时/CPU/内存/网卡/队列统计。PLCSIM 与相机网段隔离后，项目唯一发现并绑定一台 `MV-CS020-60GM`（序列号 `DB1888674`、相机 `192.168.11.117`、主机接口 `192.168.11.102`）。60 秒连续模式实际 58.35 fps，收到 3545 帧；参数完整回读一致，帧隙、超时、不完整帧、池耗尽和队列丢弃均为 0。首次软件触发真机测试暴露触发命令与阻塞取帧争用设备互斥锁；修复为采集线程按可取消周期执行“软件触发→限时取帧”并增加自动化后，20 秒复测收到 21 帧、实际 0.984 fps、0 超时。20 秒慢消费者真机测试仍保持 58.34 fps，并在容量 16 的队列上丢最旧 841 帧，证明不反压采集。ON Debug 构建和非硬件 CTest 22/22 通过，通用单元入口 149 项。路线图规定的“自动化适配层测试通过、至少一台目标相机完成功能验证”均已满足，M3-05/M3 硬件门禁解除。四台实体相机生产吞吐、物理断链/交换机恢复、服务重启和 M4 真实预览/日志并发尚未执行，作为 M5/M9 前的生产等价验证保留，不宣称通过。

测试场景：

- 目标型号发现和序列号绑定；
- 单路的目标帧率和带宽；
- 参数写入回读；
- 软触发和实际配置的触发模式；
- 拔插相机网线、交换机链路中断、服务重启；
- 统计帧率、丢帧、超时、CPU、内存和网卡带宽；
- 运行期间确认预览/日志不会阻塞采集。

退出门禁 M3：

- 自动化适配层测试通过；
- 至少一台目标相机完成功能验证；

## 10. M4：Qt 配置客户端、监测和低帧率预览

目标：提供不影响后台服务的本机运维界面。

### M4-01 UI 状态模型和导航骨架

状态：`completed`

负责人：Codex

开始日期：2026-08-03

完成日期：2026-08-03

执行记录：`.agent/plans/m4-01-ui-state-navigation.md`

完成证据（2026-08-03）：`PaperBreakEdgeConsole` 已从仅托盘客户端扩展为默认 1280×800 的 Qt Widgets 运维主窗口，包含持续可见的八项顶部状态、十二项固定导航、四路相机卡片、资源/检测/上传摘要及最近活动报警；尚无 IPC 的相机、算法、事件和上位机数据均显示对应后续里程碑占位，不伪造运行值。`ClientStateStore` 通过既有 `system.getStatus/getVersion/getMetrics` 与 `alarm.list` 并行同步四类独立快照，每类只有一个在途请求，按连接代次拒绝旧响应，断线保留最后值但标记过期，失败相互隔离并可在一秒刷新中恢复；报警推送与在途查询重叠时合并为一次后续刷新。Release 并发测试暴露 QLocalSocket 快速写空后服务端有界出站队列可能漏掉后续排水，已在 IPC 事件线程的 flush 后增加零延迟复核，状态同步重复 20 轮通过。Debug/Release 全量构建和非硬件 CTest 均为 22/22，通用 unit 入口 151 项；离屏 UI smoke、格式检查和 `git diff --check` 通过。未执行实体相机或真实预览测试，未开始 M4-02。

建立总览、实时预览、相机配置、算法配置、事件配置、存储配置、上位机配置、设备状态、报警、事件、日志和维护页面骨架；UI 只通过 IPC 获取和修改服务状态。主窗口顶部持续显示服务、时间、工控机编号、上位机、正常相机数、报警数、数据盘和软件版本；总览显示四路相机卡片、检测状态、资源指标、待上传数量和最近报警。

### M4-02 系统托盘和服务状态

状态：`completed`

负责人：Codex

开始日期：2026-08-03

完成日期：2026-08-03

执行记录：`.agent/plans/m4-02-system-tray-service-status.md`

完成证据（2026-08-03）：Console 托盘已按连接、服务状态和活动报警最高等级映射为绿/黄/红/灰四色；连接中断、同步中或快照过期始终优先显示灰色，旧值不会伪装为实时状态。右键菜单固定包含打开控制台、当前状态、预览开关、重启服务、事件目录、诊断包、关于和退出；双击恢复主窗口，窗口可反复关闭/恢复，“退出界面”只停止客户端。预览和诊断包生成分别严格保留给 M4-03/M4-06，菜单明确标注并禁用。新增只读 `system.getLocations` 由服务解析有效配置中的事件根目录，客户端按连接代次同步并只打开该路径。Windows SCM 控制拆为复用适配目标，重启在单个可取消工作任务中按 30 秒总截止时间执行停止、启动和状态确认，不阻塞 Qt 线程，权限/超时/取消均返回稳定错误。Debug/Release 全量构建和非硬件 CTest 均为 22/22，通用 unit 入口 157 项；离屏 smoke 覆盖八项菜单和 20 次窗口关闭/恢复，格式检查与 `git diff --check` 通过。未执行已安装生产服务重启、实体相机、真实预览或实际诊断包验证，不宣称通过。

实施：

- 绿色、黄色、红色、灰色状态；
- 打开控制台、状态、预览开关、重启服务、事件目录、诊断包、关于和退出；
- 退出界面不停止服务；
- 托盘通知仅作辅助，关键报警必须在页面保留。

测试：托盘状态映射、界面反复开关、服务断开时灰色且不展示未标记的过期数据。

### M4-03 预览服务端分支

状态：`completed`

负责人：Codex

开始日期：2026-08-03

完成日期：2026-08-03

执行记录：`.agent/plans/m4-03-preview-service-branch.md`

完成证据（2026-08-03）：新增 `PreviewRuntime`，每路相机使用容量为 1 的待编码槽并按 2～5 fps 抽样；没有订阅者时不会进入 JPEG 编码，慢编码时旧待编码帧被替换并计数。OpenCV 缩放/JPEG 编码仅在专用 `jthread` 执行，失败仅丢弃该预览帧；服务 IPC 提供 `preview.subscribe`/`preview.unsubscribe`，以连接内部标识定向发送 `preview.frame`，每订阅/相机经既有有界推送队列按合并键只保留最新待发送帧。Debug/Release 均完成全量构建；单元入口 160 项及排除既有 OpenCV DLL 安装扫描后的 18/18 CTest 通过。完整 CTest 的安装树扫描在外部 OpenCV Windows Pack 环境中未能解析 `opencv_world4120[d].dll`，未修改无关安装逻辑。未执行实体相机、真实服务采集输入或 M4-04 Qt 显示验证；默认配置未启用相机，服务不会伪造预览帧。

实施：

- 与原始采集解耦抽样，默认 2～5 fps；
- 无订阅者时不编码；
- 降分辨率后异步 JPEG 编码；
- 每个订阅/相机只保留最新帧，旧帧丢弃并计数；
- 二进制 IPC 负载设置大小上限；
- 最小化时可降帧率；
- 支持亮度、帧率、帧号、状态、ROI 和检测结果元数据。

测试：慢客户端、断线、四路订阅、编码失败、队列满；比较预览开/关时原始采集统计。

### M4-04 单路和四宫格预览

状态：`completed`

负责人：Codex

开始日期：2026-08-03

完成日期：2026-08-03

执行记录：`.agent/plans/m4-04-preview-client.md`

完成证据（2026-08-03）：控制台预览页已接入独立 IPC 预览客户端，支持四路 2×2 显示、1:1/自适应缩放、全屏、暂停/恢复显示、JPEG 抓图及帧号、预览 fps、相机/检测状态覆盖层。客户端只接受当前连接代次且属于当前订阅集的有效 JPEG 帧；暂停会发送 `preview.unsubscribe`，不向后台发送采集控制命令，断线后不把最后画面伪装为实时数据。预览连接与状态同步连接隔离，均使用既有有界 IPC 队列。Debug/Release 均构建成功；在补齐 Qt/OpenCV 运行时 PATH 后，非安装树扫描的 Debug/Release CTest 均通过。安装树扫描仍受外部 OpenCV Windows Pack 无法解析 `opencv_world4120[d].dll` 限制；未执行实体相机或真实预览验证。

缺陷修复证据（2026-08-03）：实际运行发现单路 `CAM01` 配置仍会被 Console 默认四路订阅整体拒绝，同时相机控制运行时只启动设备取流但没有持续取帧并提交 `PreviewRuntime`。现已让订阅集合跟随相机配置快照并覆盖在途切换竞态；服务端以每路固定 8 缓冲帧池、容量 4 丢最旧队列和可停止转发线程补齐 `CameraControlRuntime → PreviewRuntime` 数据闭环，采集中参数更新会停止并恢复该通道。全量 Debug 构建与 CTest 23/23 通过，unit 171 项；未执行实体相机可见画面验证，不宣称真机通过。

实现单画面、2×2、全屏、自适应、1:1、暂停显示、抓图、覆盖层、预览帧率和分辨率选择。暂停只停止界面更新/订阅，不停止后台采集。

### M4-05 相机配置与实际值回显

状态：`completed`

负责人：Codex

开始日期：2026-08-03

完成日期：2026-08-03

执行记录：`.agent/plans/m4-05-camera-configuration.md`

完成证据（2026-08-03）：服务实现相机查询与控制命令，并以管理员权限、停止令牌、严格 DTO、配置乐观修订/原子保存和最多四路会话约束写操作；生产服务固定注入 MVS 提供者，部署不完整时明确返回不支持。控制台相机页可显示配置槽位、发现设备、连接/采集状态、保存值和设备实际回读值，支持完整参数编辑、危险操作二次确认，并区分已保存、已下发、已应用、失败和需重启。测试专用 Mock 覆盖发现、连接、参数回读/更新、采集、快照、软件触发、权限、停止和错误输入；Debug/Release 全量构建和完整非硬件 CTest 均通过，通用 unit 入口 164 项通过。安装树运行时依赖搜索已显式包含 OpenCV 目录，安装后的服务和控制台启动检查通过。

整改证据（2026-08-03）：统一 `camera.discover` 的型号、序列号、IP、主机网卡和独占访问字段；空配置不再吞掉发现结果或操作错误，连接后自动发现并保留手动刷新。新增管理员 `camera.bind`，绑定前重新枚举、校验批准型号/槽位/序列号/独占访问，连接回读并断开成功后才原子保存，返回拓扑待重启语义。相机页分开呈现配置槽位和网络设备，拓扑待重启时禁用设备操作；顶部改为“正常/已配置相机”。控制端新增持久化“跟随系统/浅色/暗黑”应用级主题并覆盖禁用态与对话框，消除浅色背景白字。Debug/Release 预设统一为真实相机构建。最新只读真机探测和生产服务 IPC 均发现 `MV-CS020-60GM`、序列号 `DB1888674`、相机 `192.168.11.115`、主机网卡 `192.168.11.102`，且探测时 `exclusiveAccessAvailable=true`；临时配置仍为空。当前执行令牌不是提升后的管理员，真实 `camera.bind` 按设计返回 `IPC_UNAUTHORIZED`，因此未执行真机绑定、参数写入或取流验证，也未改动生产配置。

实施 `camera.list/discover/bind/connect/disconnect/start/stop/getConfig/updateConfig/captureSnapshot/softwareTrigger` 及对应 UI；覆盖逻辑编号、安装位置、型号、序列号、IP、状态、曝光、增益、帧率、ROI、像素格式、触发和网络参数；危险操作二次确认；参数展示区分“已保存、已下发、已应用、失败、需重启”，最终显示服务回读的实际值。

### M4-06 状态、报警、日志和诊断

状态：completed

负责人：Codex

开始日期：2026-08-04

完成日期：2026-08-04

执行记录：`.agent/plans/m4-06-status-alarm-log-diagnostics.md`

完成证据（2026-08-04）：服务新增系统、IPC、数据库、相机、算法及未初始化业务来源的统一指标快照；相机采集统计从既有有界采集工作线程只读暴露，实际帧率、帧缺口、超时、最近帧时间和带宽不再被误标为不可用，尚未接入的 M5/M6/M8 或设备可选数据明确返回 `available=false`。控制台以第四条隔离 IPC 连接每秒同步完整指标、当前/进程内历史报警和最近日志，支持活动范围、最低等级、来源/分类筛选、管理员确认、详情、报警 CSV 及断线过期标识。维护页和托盘均可请求 `system.exportDiagnostics`；服务构建固定 9 条目的 8 MiB 有界 ZIP，递归脱敏 credential/certificate reference、password、token、secret/private-key 字段并记录审计，客户端用容量 1 的单工作线程和 `QSaveFile` 原子落盘。Debug/Release 构建通过，两个配置的通用 unit 各 173 项及离屏 Qt smoke 均通过；完整 CTest 在 Release 为 22/23、Debug 为 21/23，唯一共同失败 `service_console_smoke` 因机器上已有不可读取详情的 `PaperBreakEdgeService` 进程占用固定 IPC（Win32 5），Debug 的安装树测试另因该进程占用 MVS DLL 目标而失败，未擅自终止现有服务。格式检查确认本任务文件通过后，被未修改的 `src/console/src/preview_client.cpp` 既有格式问题阻断。未执行实体相机、已安装生产服务或人工点击导出验证，不宣称通过。

实施系统/相机/算法指标、当前/历史报警、确认、筛选、最近日志和诊断包导出；导出配置必须脱敏。

退出门禁 M4：

- Mock 四路单画面/四宫格可用；
- 预览开启和慢 UI 不造成采集队列持续积压；
- 界面关闭不影响服务，服务重启后自动恢复；
- 配置结果明确显示实际生效值；
- 断线时过期状态有清晰标识。

## 11. M5：内存缓存与可靠事件链

目标：先用模拟检测器完成候选生成、前后冻结、可靠落盘、索引和恢复的闭环。

### M5-01 每路内存环形缓存

状态：`completed`

负责人：Codex

开始日期：2026-08-04

完成日期：2026-08-04

执行记录：`.agent/plans/m5-01-memory-ring.md`

完成证据（2026-08-04）：`paperbreak_event` 新增每相机固定槽位 `MemoryRing`、按时长/帧数规划的溢出安全预算校验、闭区间只读窗口租约、会话序号缺口统计和不足状态转换通知。环满只覆盖最旧帧；租约只复制 `FrameView` 并共享固定相机池中的原始像素缓冲，活动租约数和租约帧引用总数均有硬上限，关闭/销毁环后既有租约仍可读取。8 项定向单元测试覆盖回绕、精确边界、真实固定帧池耗尽、重叠窗口共享、29.97 fps 换算、超预算拒绝、顺序错误、不足通知及关闭时仍有引用。Debug/Release `/W4 /WX` 全量构建和两套非硬件 CTest 均为 23/23，unit 入口 181 项；本任务 C++ 文件格式与 `git diff --check` 通过，全仓格式目标仍被未修改的 `src/console/src/preview_client.cpp` 既有格式问题阻断。未执行实体相机或生产等价内存压力测试；当前 M4 相机控制路径的 8 帧临时预览池尚未接入本组件，生产装配必须在后续事件运行时接线时使用规划结果，不能宣称已有默认 10 秒真机缓存。

实施：

- 固定容量对象池，按时长/帧数换算并校验内存预算；
- 正常覆盖最旧帧；
- 事件触发后通过受控引用或所有权转移保护窗口；
- 输出实际缓存秒数、占用和不足报警；
- 多个事件重叠时共享帧，容量不得无界增长。

测试：回绕、精确边界、池耗尽、多个重叠窗口、不同帧率、关闭时仍有事件引用。

### M5-02 模拟检测器和人工触发

状态：`completed`

负责人：Codex

开始日期：2026-08-04

完成日期：2026-08-04

执行记录：`.agent/plans/m5-02-mock-detector-manual-trigger.md`

完成证据（2026-08-04）：`paperbreak_algorithm` 新增 M5 最小 `ITriggerDetector`、`TriggerResult`、检测区域和稳定触发来源契约，人工来源精确序列化为 `ManualTest`；独立 `paperbreak_algorithm_mock` 只链接算法接口，提供人工、固定单调时间周期、相邻帧平均灰度变化和 Mono8 ROI 纸幅占比四种确定性触发。人工请求使用容量 1 的原子槽，重复请求合并且错误帧不消费请求；每实例绑定逻辑相机，校验不完整帧、序号、单调时间、像素格式、stride 和 ROI。8 项定向测试覆盖四种模式、精确阈值/周期边界、stride 填充、有界人工请求、非法配置及错误后恢复；Debug/Release `/W4 /WX` 全量构建和两套非硬件 CTest 均为 23/23。任务文件 clang-format 与 `git diff --check` 通过，全仓格式目标仍被未修改的 `src/console/src/preview_client.cpp` 既有格式问题阻断。未接入 M5-03 状态机、服务配置、IPC/UI，也未执行实体相机测试。

实现手动、固定周期、灰度变化和 ROI 占比触发；人工事件写入 `triggerSource=ManualTest`。检测器只依赖算法接口，不依赖相机实现。

### M5-03 候选事件状态机

状态：`completed`

负责人：Codex

开始日期：2026-08-04

完成日期：2026-08-04

执行记录：`.agent/plans/m5-03-candidate-event-state-machine.md`

完成证据（2026-08-04）：`paperbreak_event` 新增固定最多四路的线程安全 `CandidateEventManager`，实现 Idle、Suspicious、Candidate、Confirmed、Rejected、Timeout、连续异常精确阈值、单调时间超时、版本化确认/拒绝、重复结果/命令幂等和确定性停止。首次 Candidate 使用进程内单调 UUIDv7 生成不可变 EventId，记录首次异常及候选帧/双时钟，立即持有 M5-01 前缓存租约，明确报告完整性并标记后帧收集已开始；通知在锁外同步发布且异常隔离计数。9 项定向测试覆盖全部转换、缓存保护、精确超时、墙上时间跳变、版本冲突、四路并发唯一 ID、错误恢复、回调异常、非法配置和停止；Debug/Release `/W4 /WX` 全量构建和两套非硬件 CTest 均为 23/23，Debug unit 入口 198/198。本任务 C++ 文件 clang-format 与 `git diff --check` 通过；全仓格式目标仍被未修改的 `src/console/src/preview_client.cpp` 既有格式问题阻断。未实现 M5-04 的精确后窗口/事件合并，也未接入服务配置、IPC/UI 或执行实体相机测试。

实现 Idle、Suspicious、Candidate、Confirmed、Rejected、Timeout；Candidate 产生时立即分配事件 ID、记录触发时间/帧、保护前缓存、收集后帧并发通知。

测试：状态转换、连续帧阈值、超时、拒绝、并发相机触发、重复触发幂等和服务停止。

### M5-04 前后窗口冻结和事件合并

状态：`completed`

负责人：Codex

开始日期：2026-08-04

完成日期：2026-08-04

执行记录：`.agent/plans/m5-04-event-window-merge.md`

完成证据（2026-08-04）：`paperbreak_event` 新增固定最多四路相机、四个活动事件和每事件 16 条触发的线程安全 `EventWindowManager`；首个候选立即按单调时间保护所有绑定相机的前窗口，活动窗口或精确 `mergeGap` 边界内的同/跨相机候选沿用首个规范 EventId、保留全部来源候选 ID 与原始触发并扩展窗口，乱序候选可桥接两个活动聚合。窗口封闭时合并前租约与滚动环后段，按时间/序号排序去重；`maxEventSeconds` 硬截断、墙上时间仅作展示，缓存历史、序列缺口或租约容量不足均保留可用帧并标记 `EVENT_BUFFER_INCOMPLETE`。9 项定向测试覆盖触发帧 N、闭区间首尾、墙上时间跳变、精确合并边界、乱序桥接、跨相机四线程、最大时长、固定容量、停止和三类缓存不足；Debug/Release `/W4 /WX` 全量构建、两套非硬件 CTest 23/23 及通用 unit 207/207 均通过。本任务文件 clang-format 与 `git diff --check` 通过；全仓格式目标仍被未修改的 `src/console/src/preview_client.cpp` 既有格式问题阻断。未接入服务配置/IPC/UI，未实现 M5-05 及之后落盘，也未执行实体相机测试。

实施：

- 按单调时间定位 `[T-pre, T+post]`；
- 墙上时间用于展示和目录命名；
- 限制 `maxEventSeconds`；
- 在 `mergeGapSeconds` 内合并重叠事件；
- 冻结只保护数据，不暂停采集；
- 缓存不足时保留可用范围并产生明确告警/清单标记。

测试：触发帧 N、首尾边界、时间跳变、窗口重叠、跨相机和缓存不足。

### M5-05 关键帧与 JPEG

状态：`completed`

负责人：Codex

开始日期：2026-08-04

完成日期：2026-08-04

执行记录：`.agent/plans/m5-05-keyframes-jpeg.md`

完成证据（2026-08-04）：`paperbreak_event` 新增确定性 `KeyFrameSelector`，从 M5-04 冻结窗口和有界逐帧证据中选择正常参考、最早异常、候选触发、最大变化、最高置信、正式确认和事后状态七类原因；每项均保留相机、相机帧号、会话序号、双时钟、几何和像素格式，同一物理帧命中多个原因时合并原因并只编码一次，证据缺失明确返回不完整而不伪造。`KeyFrameJpegRuntime` 使用单工作线程和预分配固定槽位 FIFO（默认 32、硬上限 256），事件内任务整批原子接收或拒绝，提交不等待编码；编码/回调异常隔离，快照暴露深度、容量、高水位、提交/完成/拒绝/失败计数，停止时排空已接受任务并支持截止时间。新增 `paperbreak_event_codec` 私有隔离 OpenCV，对 Mono8、Mono10、Mono12、Bayer RG8 执行有输入/尺寸/JPEG 字节上限的实际编码，公开接口不泄漏 OpenCV 类型。7 项定向测试覆盖七类追溯、稳定并列、缺失/非法证据、容量整批拒绝、失败恢复、确定性停止、四种像素格式实际 JPEG 解码和输出上限；Debug/Release `/W4 /WX` 全量构建和两套非硬件 CTest 均为 23/23，Debug 通用 unit 214/214。任务文件 clang-format 与 `git diff --check` 通过；全仓 `format-check` 仍首先被未修改的 `src/console/src/preview_client.cpp` 既有格式问题阻断。未接入 M5-06 目录/manifest、M5-09 服务配置/IPC/UI，未写磁盘，也未执行实体相机或生产等价性能测试。

选择正常参考、最早异常、触发、最大变化、最高置信、确认和事后帧；每个关键帧可追溯到相机、帧号、时间和选择原因。JPEG 在独立工作线程生成。

### M5-06 事务式事件目录和 manifest

状态：`completed`

负责人：Codex

执行记录：`.agent/plans/m5-06-transactional-event-directory.md`

实施：

- 使用同卷临时目录写入；
- 原始帧、关键帧和元数据完成后计算校验；
- 最后生成 manifest 并原子重命名；
- 写入任务队列有容量，满时产生 Critical 报警；
- 启动扫描未完成目录，恢复或明确标损；
- 正式目录提交前不能被读取/上传。

测试：各写入点故障注入、磁盘满、权限拒绝、校验不匹配、进程中断残留、中文路径。

完成证据（2026-08-04）：`paperbreak_storage` 新增 schema v1 事务式事件文件事实源，将 M5-04 冻结原始帧、M5-05 JPEG 关键帧和完整事件元数据写入事件根内唯一 `.transactions` 目录；逐文件执行长度和 SHA-256 写后读回校验，`manifest.json` 最后生成，Windows 实现使用 `CREATE_NEW`、`FlushFileBuffers` 和同卷 `MoveFileExW` 原子公开到 `YYYY/MM/DD/EventId`，不覆盖正式目录。公开校验入口拒绝事务/隔离/越界路径，严格检查 manifest 必需字段、目标日期、文件索引、长度和摘要。启动恢复对完整残留复验后提交，对缺清单、高版本 schema、摘要不匹配或目标冲突写 `recovery.json` 并移动到 `.quarantine`，不删除证据。`EventPersistenceRuntime` 使用单工作线程和预分配固定事件槽（默认 8、硬上限 64），提交不等待磁盘；满载/停止返回 Critical `EVENT_WRITE_FAILED`，写入与回调异常隔离，停止时排空已接受任务并支持截止时间。10 项定向测试覆盖标准 SHA-256、中文/空格路径、非法标识/预算/关键帧追溯拒绝、四类文件写入和最终 rename 故障、磁盘满、权限拒绝、写后与恢复校验不匹配、完整残留恢复、缺失/高版本 manifest 隔离、正式目录提交前不可读、容量精确边界及确定性停止。Debug/Release `/W4 /WX` 全量构建及两套非硬件 CTest 均为 23/23，Debug 通用 unit 217/217；任务文件格式检查和 `git diff --check` 通过。全仓 `format-check` 仍被未修改的 `src/console/src/preview_client.cpp` 既有格式问题阻断，静态分析在进入 storage 前被未修改的 `src/event/src/key_frame.cpp` 两条既有 C6011 阻断。未接入 M5-07 SQLite/M5-09 服务配置、IPC 和报警，也未执行实体相机、真实进程强杀、物理断电或生产磁盘性能测试。

### M5-07 SQLite 元数据和迁移

状态：`completed`

负责人：Codex

执行记录：`.agent/plans/m5-07-sqlite-metadata-migrations.md`

实施：

- 建立 events、event_cameras、key_frames、event_files、upload_jobs、device_status_history、config_history、alarm_history、audit_logs；
- SQLite 只存元数据；
- 从 schema 版本 1 开始提供迁移和回滚/备份策略；
- 事件提交与数据库索引失败之间有可恢复对账流程；
- 支持分页和按时间、状态、相机筛选。

测试：新建、迁移、重复迁移、损坏检测、备份恢复、目录/数据库不一致对账。

完成证据（2026-08-04）：`paperbreak_storage` 新增不暴露 SQLite 类型的 schema v1 元数据数据库，使用单连接互斥协调迁移和关键事务，建立 events、event_cameras、key_frames、event_files、upload_jobs、device_status_history、config_history、alarm_history、audit_logs 九张 STRICT 表及外键/查询索引，图像仍只保存在 M5-06 事件目录。新库和 user_version=0 旧库通过单事务迁移，既有旧库迁移前生成不可覆盖的一致备份；重复打开幂等，高版本、quick_check 损坏和迁移失败分别稳定拒绝，显式恢复经同卷临时库校验后原子替换且保留备份。已提交目录先执行 manifest、长度和 SHA-256 校验，再原子 upsert 事件及相机/关键帧/文件子索引；分页固定上限 200，支持时间、状态、相机筛选。固定最多 10000 个事件的启动对账以目录为事实源补建/刷新索引，并把数据库孤儿标为 Missing，不删除目录或记录。7 项定向测试覆盖九表新建、0→1/重复/失败回滚迁移、迁移前备份、高版本、损坏检测、备份恢复与拒绝覆盖、中文空格路径、稳定分页筛选、双向不一致幂等对账、校验损坏及已验证但字段畸形 manifest 不产生半索引。Debug/Release `/W4 /WX` 全量构建、两套非硬件 CTest 23/23、storage 默认 MSVC 静态分析、任务文件格式检查和 `git diff --check` 均通过；全仓 `format-check` 被未修改的 `src/pipeline/include/paperbreak/pipeline/preview.hpp` 既有格式问题阻断。未接入 M5-09 服务配置、IPC/UI、报警写入或上传执行器，未执行实体相机、真实进程强杀、断电和生产磁盘性能测试。

### M5-08 存储水位和保留策略

状态：`completed`

负责人：Codex

开始日期：2026-08-04

完成日期：2026-08-04

执行记录：`.agent/plans/m5-08-storage-watermarks-retention.md`

实施：

- warning、critical、stop 水位；
- 优先清理已上传且可删除的最旧事件；
- 默认不删未上传或人工锁定事件；
- 先更新删除状态，再执行可恢复删除/对账；
- 清理临时文件，支持天数和容量上限；
- stop 水位禁止新增大文件但服务仍持续监测报警。

完成证据（2026-08-04）：`paperbreak_storage` 新增同步、有界 `StoragePolicyManager`，采样事件卷总量/可用量并按精确边界输出 Normal、Warning、Critical、StopSave 快照；Critical 关闭普通滚动写准入，StopSave 以 Critical `STORAGE_LOW_SPACE` 拒绝新增大文件，检测与报警调用契约保持可用。SQLite 升级到 schema v2，迁移前不可覆盖备份，新增独立 `event_retention` 保存人工锁定、明确允许删除、manifest 字节和 Active/DeletePending/DeleteFailed/Deleted 状态，不改写不可变 manifest。Warning、保留天数和事件容量上限只按最旧顺序选择 `Uploaded + deletionAllowed + !locked` 的 Present 事件；每轮最多 200 个事件，显式临时根最多 16 个且扫描/删除最多 10000 项。删除先 CAS 落库，再同卷移动到 `.deletions`、递归删除并事务式完成状态；移动失败、删除失败、移动后中断、物理删除后未落库，以及期间先执行目录对账均可在后续维护轮次恢复。5 项策略测试与 8 项数据库测试共 13/13，Debug/Release `/W4 /WX` 全量构建、两套非硬件 CTest 23/23、Debug 通用单元入口 230/230、storage 默认 MSVC 静态分析、任务文件格式检查和 `git diff --check` 均通过。全仓 `format-check` 仍被未修改的 `src/pipeline/include/paperbreak/pipeline/preview.hpp` 既有格式问题阻断。M5-09 服务周期调度、配置、报警、IPC/UI 尚未接入，未执行实体相机、真实进程强杀/断电或生产 NVMe 水位与性能测试。

### M5-09 事件配置、查询、复核与导出

状态：`completed`

负责人：Codex

开始日期：2026-08-04

完成日期：2026-08-04

执行记录：`.agent/plans/m5-09-event-review-export.md`

实施：

- 实现 `event.list/get/manualTrigger/confirm/reject` IPC 命令；
- 事件配置页支持前/后秒数、最大时长、合并间隔、关键帧数、原始数据、预览视频、上传和保留策略；
- 事件页支持分页、时间/状态/相机筛选、状态、触发相机、置信度、上传状态和缩略图；
- 支持打开已提交事件目录、查看 manifest、确认/误报标记和导出；
- 导出只读取已原子提交的事件，并验证 manifest 与文件校验；
- 提供测试用事件检查器或等价集成测试，能按 manifest 顺序读取原始帧并追溯关键帧；
- `retryUpload` 在 M8 上传队列完成后启用，M5 阶段明确显示尚不可用，不伪造成功。

测试：分页边界、筛选、并发状态更新、未提交目录不可见、损坏事件提示、导出中途失败和中文目标路径。

退出门禁 M5：

- 给定触发帧 N，可验证前后窗口、序列连续性和关键帧来源；
- 人工触发不会停止采集，重叠事件按策略合并；
- manifest 校验正确，未完成事件可恢复或标损；
- SQLite 可查询且迁移测试通过；
- 事件可查询、复核、校验、导出并按帧顺序检查；
- 磁盘满和写入失败不会导致采集线程执行 I/O 或无限阻塞。

完成证据（2026-08-04）：服务组合根现已把最多四路相机帧以容量 64 的非阻塞队列接入每相机内存环、M5 mock detector、候选/窗口、关键帧 JPEG、容量 8 的事件写队列、原子事件目录和 SQLite schema v3；默认帧池提升到 2048，并在启动/重配置时校验前后窗口预算。服务启动恢复/对账后开放 IPC，30 秒周期执行水位和保留维护，StopSave 拒绝新增大文件但不停止检测。数据库提供单事件详情和带 `review_revision` 的 Confirmed/Rejected 乐观并发及幂等语义，对账保留人工复核。只读检查器只接受正式提交目录，验证 manifest、SHA-256、原始帧顺序/缺口和关键帧追溯，并以单文件 128 MiB、总计 64 GiB 上限流式生成 ZIP64。事件 IPC 增加配置读取/更新、分页筛选、详情、独立 manifest、人工触发、复核和受控缓存导出；Qt `EventClient` 与两张页面提供全部路线图字段、状态/相机/置信度/上传状态/缩略图、manifest、目录、复核以及中文空格路径的 `QSaveFile` 原子导出，`retryUpload`、正式算法和预览视频生成均明确标注阶段不可用。Debug/Release `/W4 /WX` 全量构建和两套非硬件 CTest 均为 23/23，33 项相关单元/模拟集成测试、默认 MSVC 静态分析、25 个任务 C++ 文件格式检查和 `git diff --check` 通过；全仓 `format-check` 仍首先被未修改的 `src/pipeline/include/paperbreak/pipeline/preview.hpp:6` 既有格式问题阻断。未执行实体相机、四路生产内存/吞吐、真实 NVMe 水位、SCM 强杀或断电测试，不能声明硬件门禁通过。

## 12. M6：正式算法接口与初版算法

目标：在不改变采集和事件模块的情况下替换检测实现，并让异常可隔离、性能可观测。

### M6-00 算法验收基线

状态：`blocked`

负责人：Codex

开始日期：2026-08-04

执行记录：`.agent/plans/m6-00-algorithm-acceptance-baseline.md`

在编码前确定：

- 已标注验证数据集和版本；
- 纸幅/断纸类别定义；
- 准确率、召回率、误报率或业务批准指标；
- 单帧耗时、吞吐和硬件预算；
- 算法模型文件来源、版本和校验方式。

无上述基线时可以实现接口和传统视觉原型，但不能宣称正式算法通过验收。

阻塞记录（2026-08-04）：已建立 `docs/validation/m6-00/` 版本化门禁合同、四类标签与事件级评估规范、数据集 manifest schema/模板、四方审签模板和 CTest 静态检查；已知四路各 60 fps 的理论入口为 240 frame/s，传统视觉无外部模型及以后引入制品时的版本/SHA-256 规则已明确。仓库仍没有冻结标注数据集、业务批准的召回/精确/每相机小时误报/检测延迟阈值、目标 ROI/像素格式/工控机 CPU 与内存预算或责任人审签，因此 DEC-006 尚未解除，M6-00 不得标记完成，后续算法只能保持“原型”。

### M6-01 插件接口与生命周期

状态：`completed`

负责人：Codex

开始日期：2026-08-04

完成日期：2026-08-04

执行记录：`.agent/plans/m6-01-detector-plugin-lifecycle.md`

完成证据（2026-08-04）：`paperbreak_algorithm` 新增不暴露 Qt/OpenCV/MVS 类型的 `DetectorConfig`、`DetectionResult`、`DetectorInfo`、`IBreakDetector`、容量 16 的编译期插件注册表和串行 `DetectorHost`；结果模型保持 M5 事件触发字段源码兼容并补齐异常类型、置信度、区域/面积、变化量、实测耗时、实现/模型版本及调试指标。宿主只在创建、初始化和信息校验全部成功后装载；热更新通过“候选实例按旧配置初始化、在候选上应用新修订、成功后交换”保证失败/异常时保留旧实例和旧配置。工厂及全部生命周期异常转换为稳定业务错误，同步处理超预算在调用返回后丢弃结果并记录调用/成功/失败/超时和最大耗时，不伪装成可安全抢占的硬超时。Mock 已实现初始化、热更新、重置和版本查询且保持手工/周期/灰度/ROI 行为；ADR-016 明确首版是同工具链进程内编译期边界而非跨 DLL ABI。15 项定向测试覆盖未知/重复插件、工厂/初始化失败、强回滚、更新/处理标准及未知异常、慢处理、重置和 Mock 生命周期；Debug/Release `/W4 /WX` 全量构建及两套 CTest 均为 24/24，算法与 Mock 静态分析构建、任务 C++ 文件 clang-format 和 `git diff --check` 通过。全仓 `format-check` 仍被未修改的 `src/console/main.cpp:467` 既有格式问题阻断。未执行实体相机、冻结数据集、四路目标机算法性能或正式断纸验收；M6-00 仍 blocked，所有检测器继续标记为原型。

实现 `IBreakDetector`、DetectorConfig、DetectionResult、DetectorInfo；支持初始化、处理、热更新、重置和版本查询；插件 ABI/装载策略通过 ADR 明确，第一版可优先采用进程内编译期插件边界。

测试：加载失败、初始化失败、配置回滚、处理异常、超时/慢处理和重置。

### M6-02 传统视觉算法初版

状态：`completed`

负责人：Codex

开始日期：2026-08-04

完成日期：2026-08-04

执行记录：`.agent/plans/m6-02-classical-vision-prototype.md`

实现经批准的 ROI、灰度/变化量、纸幅占比或背景比较算法；结果至少包含异常、类型、置信、区域、面积比例、变化量、耗时、版本、原因和调试指标。

完成证据（2026-08-04）：新增独立 `paperbreak_algorithm_classical` 和编译期 `classical-vision` 插件，OpenCV core/imgproc 仅作为私有实现依赖，公开边界不暴露 OpenCV 类型。原型以零拷贝只读 Mono8/stride 视图执行最大 4,194,304 像素的 ROI 灰度、相邻均值变化、纸幅占比、首个健康帧背景比较及非异常背景学习；输出异常类型、未校准归一化置信、区域/面积、变化量、宿主实测耗时、版本、原因和 11 项调试指标。初始化、reset 和热更新清除有状态参考，配置/格式/ROI/序号/时间错误使用稳定业务错误。9 项内存帧测试覆盖插件装载、ROI/stride、三类触发、缺纸背景保护、生命周期和错误输入；Debug/Release `/W4 /WX` 全量构建与两套 CTest 均为 24/24，MSVC 静态分析目标、任务文件 clang-format 和 `git diff --check` 通过。全仓 `format-check` 仍被未修改的 `src/console/main.cpp:467` 既有格式问题阻断。未执行实体相机、冻结数据集、目标工控机四路性能或真实断纸验收；M6-00 仍 blocked，因此实现和默认阈值保持原型，未进入 M6-03/M6-04。

### M6-03 候选确认和降级

状态：`completed`

负责人：Codex

开始日期：2026-08-04

完成日期：2026-08-04

执行记录：`.agent/plans/m6-03-candidate-confirmation-degradation.md`

实施：

- 连续帧、阈值、冷却和外部信号组合；
- 算法队列积压时显式跳帧并计数；
- 单次算法异常不退出采集服务；
- 连续异常触发报警并切换到批准的降级状态；
- 配置失败保留旧实例/旧配置。

完成证据（2026-08-04）：候选状态机已实现候选/确认两级置信阈值、独立连续帧、可选外部活动信号、候选截止时间和单调时钟冷却精确边界。服务事件运行时统一经 `DetectorPluginRegistry`/`DetectorHost` 装载 Mock 与 `classical-vision`，每相机待检测队列默认容量 8，满载时只丢最旧待检测帧并保留内存事件缓存；单帧错误隔离，连续 3 次处理失败或连续 8 次积压进入可观察的 `manual-trigger-only`，人工触发继续可用。事务式重配置失败保留旧检测器和旧配置；服务监控输出实际状态、处理耗时、队列、高水位、跳帧、失败、候选、确认及拒绝计数，并将积压/降级接入稳定业务错误和报警。19 项定向候选/运行时测试通过；Debug/Release `/W4 /WX` 全量构建与两套 CTest 均为 24/24，MSVC 静态分析目标、任务文件 clang-format 和 `git diff --check` 通过。全仓 `format-check` 仍被未修改的 `src/console/main.cpp:467` 等既有格式问题阻断。未执行实体相机、冻结数据集、目标机四路性能、真实 PLC 或 7×24 验证；M6-00 仍 blocked，算法保持原型，未进入 M6-04。

### M6-04 UI 配置和结果叠加

状态：`completed`

负责人：Codex

开始日期：2026-08-04

完成日期：2026-08-04

执行记录：`.agent/plans/m6-04-ui-algorithm-configuration-visualization.md`

实现算法启用、类型、ROI、阈值、连续帧、冷却、模型、版本、设备和调试可视化；支持测试当前图像，并展示实际应用状态和性能指标。

完成证据（2026-08-04）：服务新增 `algorithm.getConfig/updateConfig/testCurrentFrame` 三项严格 IPC 命令，配置更新沿用乐观修订、审计、原子保存和事务式运行时重配置，响应同时给出保存/有效/运行时修订、检测器信息、实际状态及算法运行时汇总队列、处理耗时、失败、跳帧和候选结果指标。事件运行时按相机保留最近一张 RAII 内存帧；单帧测试在正式算法队列和候选状态机之外创建隔离检测器，生成不超过 8 MiB 的 JPEG，不写盘、不改变正式检测器指标且明确返回 `candidateCreated=false`。Qt 算法页覆盖启用、类型、ROI、两级阈值、连续帧、冷却、模型引用/版本、设备和调试开关，显示已保存/已下发/已应用、实际插件/模型/降级状态与性能表，并在测试图上叠加检测 ROI、候选类型和置信度；M6-00 原型警示始终可见。新增运行时、系统命令和 `AlgorithmClient` 三项端到端行为测试，Debug/Release `/W4 /WX` 全量构建及两套非硬件 CTest 均为 24/24，通用 unit 入口各 266 项，Qt 离屏 smoke、MSVC 静态分析、11 个任务 C++ 文件 clang-format 和 `git diff --check` 通过。全仓 `format-check` 仍被未修改的 `src/pipeline/include/paperbreak/pipeline/preview.hpp:6` 既有格式问题阻断。未执行实体相机、冻结数据集、目标工控机四路性能或人工 UI 点击验证；M6-00/DEC-006 仍 blocked，M6 退出门禁中的冻结数据集报告尚未满足，所有算法继续标记为原型，未进入 M7。

退出门禁 M6：

- 模拟检测器和初版算法可替换而不改采集/事件代码；
- 故障、积压和配置失败均有降级与指标；
- 在冻结数据集上生成可复现评估报告；
- 未满足 M6-00 指标时保持“原型”标记。

## 13. M7：NVMe 滚动缓存

目标：提供固定容量、可恢复、不会阻塞采集的磁盘滚动缓存。

### M7-01 格式和容量设计

状态：`completed`

负责人：Codex

开始日期：2026-08-05

完成日期：2026-08-05

执行记录：`.agent/plans/m7-01-nvme-format-capacity-design.md`

定义版本化块头、帧索引、原始帧区和 CRC/Hash；确定 1 秒或 5 秒块大小、预分配/写入策略、容量公式和磁盘带宽预算。不自定义复杂视频编码。

完成证据（2026-08-05）：ADR-011 已接受 NVMe v1 设计：每相机 1 秒原始无压缩块，显式小端
4096 字节头页、96 字节帧索引、原始数据预留和 4096 字节提交尾页，使用逐帧/区域 CRC32C
及末写提交标记区分完整块与断电尾块。容量按 `stride × height`、已应用最大帧率和固定索引
上限计算，每个在写临时块计入总容量；普通滚动写最多使用目标卷实测持续写带宽的 80%。机器
合同和 CTest 可复算当前 1624×1240 Mono8、60 fps 四路参考负载约 460.966 MiB/s、最低盘
持续写约 576.208 MiB/s、1000 GiB 最大块预算约保留 2149 秒。该算例不是四路/目标 NVMe
硬件验收；最终生产 ROI、stride、目标盘热稳定与并发事件性能仍须 M7-02～M7-04/M7 退出门禁
实测。本任务未实现写线程、租约或恢复，未进入 M7-02。

### M7-02 异步块写入

状态：`completed`

负责人：Codex

开始日期：2026-08-05

完成日期：2026-08-05

执行记录：`.agent/plans/m7-02-async-nvme-block-writer.md`

实施：

- 独立有界写入队列和线程；
- 写入限速、超时和错误传播；
- 总容量固定，正常删除最旧未保护块；
- 缓存盘不可用时降级内存缓存；
- critical 水位停止普通滚动缓存但保留正式事件能力。

完成证据：已实现 ADR-011 v1 文件写入、每相机固定 2 块有界队列、单写线程、限速/超时/取消、固定容量回绕、内存降级及水位准入；schema v2、帧池预算、指标、报警和公开文档已同步。Debug/Release 全量 CTest 各 26/26 通过，NVMe 定向测试 9/9 通过；详细记录见执行计划和 `docs/validation/m7-02/README.md`。未执行实体四相机、目标 NVMe、拔盘或断电验收，且未提前实现 M7-03/M7-04。

### M7-03 索引、保护和事件引用

状态：`completed`

负责人：Codex

开始日期：2026-08-05

完成日期：2026-08-05

执行记录：`.agent/plans/m7-03-nvme-index-leases.md`

按相机和时间定位块；事件窗口通过引用/租约保护相关块；事件提交后按策略释放；跨块帧序列和校验可追溯。

完成证据（2026-08-05）：已实现 schema v1 SQLite 可重建派生块索引、相机/UTC 有界查询、跨块序号缺口/重叠与提交 CRC 追踪、最多 64 条活动事件租约、既有/未来重叠块保护及零租约回绕。候选建立租约，事件目录和元数据成功提交后释放，失败时保留；全部块受保护时不删除并以 `NVME_CACHE_PROTECTED` 降级。定向测试 16/16、Debug CTest 26/26、Release CTest 25/25、MSVC 静态分析和任务文件格式检查通过；详见执行计划和 `docs/validation/m7-03/README.md`。未执行实体四相机/目标 NVMe/拔盘/断电验收，启动扫描与尾块恢复仍属于 M7-04。

### M7-04 崩溃与断电恢复

状态：`completed`

负责人：Codex

开始日期：2026-08-05

完成日期：2026-08-05

执行记录：`.agent/plans/m7-04-crash-power-recovery.md`

启动时扫描块头、索引和校验，截断/隔离不完整块，重建索引；恢复过程有时间和内存上限，不覆盖仍被事件引用的块。

测试：每个写入阶段中断、尾块损坏、索引丢失、校验错误、磁盘移除、容量回绕、事件保护竞争。

完成证据（2026-08-05）：已实现 v1 有界流式启动扫描、完整块严格接纳、连续有效 `.partial`
前缀尾页重建与双刷新原子发布、不可恢复/未知版本块非覆盖隔离、派生索引事务重建、UTC 租约
跨重启重新绑定、缓存物理占用和相机代次恢复。故障注入覆盖预分配、头、索引、负载、尾页、
标记和发布阶段，以及头/索引/负载/尾页/标记 CRC、未知版本、索引丢失、扫描上限、模拟磁盘
移除、容量回绕和事件保护竞争；详细自动化命令和结果见执行计划及
`docs/validation/m7-04/README.md`。恢复/NVMe/事件定向测试 23/23、Debug 和 Release CTest 各
25/25、两种配置通用单元入口各 291 项，MSVC 静态分析及任务文件格式检查通过。未执行实体四
相机、目标 NVMe、物理拔盘/断电或持续热稳定测试，因此 M7 退出门禁中的目标硬件性能项仍未
满足，未进入 M8。

退出门禁 M7：

- 缓存占用不超过配置上限；
- 正常块循环覆盖，事件块不被覆盖；
- 磁盘故障不阻塞采集并能降级；
- 故障注入证明残留块可恢复或隔离；
- 目标 NVMe 上持续写入时采集统计满足预算。

## 14. M8：上位机通信与离线补传

目标：在上位机离线时不影响本地检测，恢复后可靠、幂等地补传。

### M8-00 协议评审门禁

状态：`completed`

负责人：Codex

开始日期：2026-08-05

完成日期：2026-08-05

执行记录：`.agent/plans/m8-00-uplink-protocol-simulator.md`

M8-00 已确认：

- REST/WebSocket 端点采用明文、无鉴权方案，不使用 TLS 或证书轮换；
- 消息 schema、版本兼容、幂等键和服务端确认语义；
- 文件分块、断点状态、校验和重试规则；
- 预览通过 WebSocket 跨机发送，但只保留最新帧且不持久化；
- 带宽限制和上传优先级；
- PLC 是否进入本阶段以及采用何种协议。

实现证据（2026-08-05）：新增不暴露 Qt 类型的 Uplink v1 DTO/严格 JSON 与二进制预览编解码，以及默认构建的 `PaperBreakUplinkSimulator` Qt GUI/headless 参考服务端。模拟器支持最多 16 台设备、REST/WebSocket 回环通信、事件与 SQLite schema v1、SHA-256 分块断点上传、重启复核/损坏隔离、能力约束命令和版本化故障场景；独立 `UplinkSimulator` 安装组件不进入 PaperBreakEdge 生产安装。协议见 `docs/uplink-protocol-v1.md`，安全风险接受见 ADR-012。该证据只完成 M8-00，不代表已实现 M8-01～M8-04，也未与正式上位机或真实边缘 M8 联调。

### M8-01 传输抽象和 Mock

状态：`completed`

负责人：Codex

开始日期：2026-08-05

完成日期：2026-08-05

执行记录：`.agent/plans/m8-01-uplink-transport-mock.md`

实现 `IUplinkTransport` 和 `MockUplinkTransport`；业务层不依赖 HTTP/WebSocket 类型。Mock 可脚本化模拟离线、慢速、失败、重复确认和校验错误。

实现证据（2026-08-05）：`paperbreak_uplink` 新增强类型同步传输端口、连接状态、会话、事件元数据、文件请求和有界重复确认模型，公开头文件只依赖 C++ 标准库与领域 `Result`/Uplink v1 DTO；`BUILD_TESTING` 下新增 `paperbreak_uplink_mock`，以容量受限的 FIFO 故障脚本和覆盖式调用历史模拟离线、有上限慢响应、可重试/永久失败、重复确认及校验错误，并支持锁外命令注入和确定性断开。6 项定向测试及 Debug/Release 全量构建和 CTest 已通过。该证据只完成 M8-01；未实现 M8-02 心跳/命令编排、M8-03 持久上传调度、M8-04 Qt HTTP/WebSocket/分块适配器，也未执行真实上位机或网络联调。

### M8-02 心跳、状态和命令

状态：`completed`

负责人：Codex

开始日期：2026-08-05

完成日期：2026-08-05

执行记录：`.agent/plans/m8-02-uplink-heartbeat-command.md`

实现连接状态、心跳、指数退避、服务端命令处理、协议版本和重复命令幂等；外部命令必须复用服务端校验和审计。

实现证据（2026-08-05）：`paperbreak_uplink` 新增单工作线程 `UplinkRuntime`，完成 Uplink v1 会话校验、连接状态、连接后立即状态上报、周期心跳/状态、有上限指数退避、停止令牌/断开/截止 join，以及命令条数与字节双重有界队列；命令严格检查版本、machineId、RFC 3339 截止时间、握手能力和人工确认，最近命令结果按条数/字节有界缓存，相同 `commandId` 重放原结果、不同内容稳定冲突。`SystemCommandService` 新增远程映射入口，复用既有状态、配置、事件和相机 dispatcher；配置写入审计来源为 `uplink`，远程变更缺少审计设施时拒绝。9 项定向测试及 Debug/Release 全量构建和 CTest、MSVC 静态分析已通过。该证据只完成 M8-02；M8-03 持久上传任务、M8-04 真实 HTTP/WebSocket/分块适配器、跨重启命令结果和正式上位机/网络联调均未实现或执行。

### M8-03 持久化上传调度

状态：`completed`

负责人：Codex

开始日期：2026-08-05

完成日期：2026-08-05

执行记录：`.agent/plans/m8-03-persistent-upload-scheduling.md`

实施：

- upload_jobs 使用 SQLite；
- 优先级为报警元数据、关键帧、manifest、低码率回放、原始文件；
- 队列有容量和磁盘上限；
- 指数退避带抖动和最大间隔；
- 重启后恢复任务，不重复创建事件；
- 失败分类为可重试、永久失败和需人工处理。

实现证据（2026-08-05）：SQLite 元数据 schema 升级至 v4，`upload_jobs` 支持每事件多资源和独立报警元数据任务、幂等键、固定五级优先级、声明字节数、checkpoint、尝试次数及三类失败终态；入队在事务内同时执行未完成任务条数和待上传字节双上限检查，已完成历史只按配置上限裁剪。`paperbreak_uplink` 新增单工作线程 `PersistentUploadScheduler`，实现原子领用、带抖动和最大间隔的指数退避、重启回收在途任务、次数耗尽转人工处理以及停止令牌和确定性 join；`event.retryUpload` 复用同一任务记录执行显式重试，不创建重复事件或任务。新增 7 项上传调度定向测试，并更新迁移、系统命令与运行时测试；Debug/Release 全量构建和 28 项 CTest、MSVC 静态分析均通过。该证据只完成持久任务仓库和调度策略；M8-04 的正式 HTTP/WebSocket 适配器、文件分块/校验/带宽限制，以及真实上位机、网络故障、进程强杀、物理断电与实体相机联调均未实现或执行。

### M8-04 分块上传与校验

实现文件分块、断点续传、客户端/服务端校验、确认后状态提交；重复块和重复完成请求必须幂等；限速不能占满采集所需网络/磁盘资源。

### M8-05 PLC/现场 IO 抽象（首期预留）

定义 `IPlantIoAdapter`、PlantSignals、VisionAlarm 和 Mock；明确现场安全停机不能只依赖上位机。只有 DEC-005 已决且另行批准时才实现具体协议。

退出门禁 M8：

- Mock 断网期间事件持续保存且上传任务持久化；
- 恢复后按优先级补传；
- 服务/客户端重复请求不产生重复事件；
- 校验失败可重传且有上限；
- 真实上位机协议未提供时，明确保持“Mock 联调完成、生产联调待完成”。

## 15. M9：安全、部署、稳定性与最终验收

目标：把功能工程转为可部署、可维护、可证明满足首期定义的产品。

### M9-01 安装、升级和卸载

实施：

- 安装服务、Qt 客户端、运行时依赖和默认配置；
- 创建专用低权限服务账户与最小目录/网络权限；
- 配置自动启动和异常恢复；
- 数据、日志、缓存和事件目录可配置且升级时保留；
- 升级包签名校验；
- 卸载默认不删除生产数据；
- 安装失败可回滚。

测试：干净 Windows 10/11 x64、覆盖升级、失败回滚、卸载保留数据、非管理员拒绝。

### M9-02 安全加固

实施：

- IPC 仅本机且配置 ACL；
- 生产参数修改需要批准的管理员/应用鉴权；
- TLS、证书和密钥安全存储；
- 日志、诊断包和审计脱敏；
- 外部 JSON、IPC、网络和文件输入做大小/类型/范围校验；
- 依赖漏洞和许可证扫描。

### M9-03 健康、报警和诊断闭环

核对系统、相机、算法、存储、数据库、上位机全部指标；验证报警 raise、去重、计数、clear、ack 和历史查询；诊断包包含脱敏配置、日志、系统/相机/网络/数据库健康、报警、软件和依赖版本。

### M9-04 性能与故障注入

在目标机按固定测试配置记录：

- 四路目标相机的帧率、丢帧、带宽；
- CPU、内存、线程、句柄和所有队列深度；
- 预览开/关、事件写入、NVMe 滚动写入和上传并发场景；
- 相机掉线、上位机断网、磁盘满/移除、数据库异常、系统时间跳变；
- 服务和 Qt 进程分别崩溃后的恢复；
- 事件触发帧 N 的前后窗口和校验。

### M9-05 7×24 小时稳定性测试

要求：

- 目标工控机、四路真实目标相机和生产等价网络；
- 至少连续 168 小时；
- 周期性模拟事件、预览订阅、上位机断连和相机短时掉线；
- 记录内存/句柄趋势、重连、丢帧、队列高水位、事件和校验；
- 中途失败后修复必须重新开始完整计时，除非验收方书面批准缩减复测范围。

通过条件：无持续内存/句柄增长，无未解释服务退出，无丢失的已确认事件，所有丢帧可统计，恢复行为满足约定。

### M9-06 运维文档和发布清单

产物：

- 安装升级手册；
- 配置手册；
- 运维监控手册；
- 故障排查手册；
- 相机网络和 MVS 运行时部署说明；
- 备份、恢复和数据保留说明；
- 已知限制、第三方依赖和许可证；
- 发布版本、配置 schema、数据库 schema、事件 schema、IPC 协议兼容矩阵。

退出门禁 M9 / 首期发布：

- M0～M8 的阶段门禁全部完成；
- Windows 10/11 安装和升级验证通过；
- 四路真机性能、故障恢复和 168 小时稳定性证据通过评审；
- 需求第 18 节 15 项完成定义逐项签字；
- 所有未解决 Critical/Error 缺陷关闭；
- 发布包、校验值、签名、回滚包和运维文档齐备。

## 16. 需求追踪矩阵

| 需求章节 | 主要实现阶段 | 最终验证阶段 |
| --- | --- | --- |
| 1 建设目标、两个进程原则 | G0、M0、M1 | M9 |
| 2 技术基线与依赖 | G0、M0 | M9 |
| 3 总体架构 | G0 | 各阶段架构审查 |
| 4.1 服务生命周期 | M1 | M9 |
| 4.2 配置管理 | M1、M4 | M9 |
| 4.3～4.6 相机与帧 | M2、M3 | M9 |
| 4.7 图像处理管线 | M2、M6 | M9 |
| 4.8 低帧率预览 | M4 | M9 |
| 4.9～4.12 算法、状态机、内存缓存 | M5、M6 | M9 |
| 4.13 NVMe 滚动缓存 | M7 | M9 |
| 4.14～4.17 事件、数据库、存储 | M5 | M9 |
| 4.18 上位机通信 | M8 | M9 |
| 4.19 PLC/现场 IO | M8 抽象；具体实现待决策 | 后续批准的集成测试 |
| 4.20～4.22 健康、报警、日志 | M0、M2～M9 持续完善 | M9 |
| 5 Qt 客户端 | M4、M5、M6、M8 | M9 |
| 6 IPC | M1、M4 | M9 |
| 7 线程模型 | G0、M1、M2、M4～M8 | 每阶段并发关闭测试、M9 |
| 8 相机状态机 | M2、M3 | M9 |
| 9 推荐目录 | M0 | M0 |
| 10 配置结构 | M1 | M4、M9 |
| 11 错误码 | G0、各阶段 | M9 |
| 12 安全权限 | M1、M8、M9 | M9 |
| 13 非功能要求 | 各阶段提供指标 | M9 |
| 14 分阶段计划 | 本路线图 M0～M9 | 各阶段门禁 |
| 16 代码约束 | 所有阶段 | 代码审查、静态分析、测试 |
| 17 首期不做 | 所有阶段范围控制 | 发布评审 |
| 18 首期完成定义 | M0～M9 | M9 签字 |

## 17. 工作量与安排建议

下列为一名熟悉 C++/Qt/Windows 的工程师的粗略净开发量，不含采购、等待外部协议、算法标注和 168 小时稳定性运行时间；只能用于排期初估。

| 阶段 | 估算人日 | 主要不确定性 |
| --- | ---: | --- |
| G0 | 5～8 | 外部接口、硬件配置和依赖版本 |
| M0 | 6～10 | CI、依赖获取和 Windows 环境 |
| M1 | 15～24 | SCM、IPC 安全、原子配置和故障场景 |
| M2 | 18～28 | 对象池、并发关闭和高吞吐模拟 |
| M3 | 15～25 | MVS SDK 行为、相机/网卡可用性 |
| M4 | 22～35 | UI 细节、四路预览和状态一致性 |
| M5 | 30～45 | 事件并发、崩溃一致性和 SQLite 对账 |
| M6 | 20～40 | 数据集和算法指标，区间不含模型研发 |
| M7 | 20～32 | 磁盘格式、断电恢复和目标 NVMe 性能 |
| M8 | 20～35 | 上位机协议、明文无鉴权风险控制和断点续传 |
| M9 | 18～30 | 安装器、安全审查和缺陷修复，不含 7 天等待 |

建议优先安排：

1. 单人顺序完成 G0、M0、M1、M2；
2. M2 后可由不同人员并行推进 M3 与 M4；
3. M5～M8 按数据可靠性依赖顺序推进；
4. 从 M2 起持续积累性能和稳定性数据，不把所有风险留到 M9；
5. 真实相机、目标工控机、NVMe、交换机和上位机测试环境应在 M2 前落实，避免 M3/M9 阻塞。

## 18. 风险登记

| 风险 | 影响 | 缓解措施 | 责任阶段 |
| --- | --- | --- | --- |
| 四路相机带宽或网卡配置不足 | 无法达到目标帧率 | M3 逐路扩展压测，记录包大小、链路和 CPU；提前准备独立网卡/交换机方案 | M3 |
| 缓存内存预算未知 | OOM 或无法保存目标前置时长 | M2/M5 用分辨率×帧率×时长测算，启动时拒绝超预算配置 | M2、M5 |
| 预览/事件 I/O 反压采集 | 丢帧和候选遗漏 | 独立有界队列、最新帧策略、指标和压力测试 | M2、M4、M5 |
| 进程崩溃留下半事件 | 数据不可用或上位机误读 | 临时目录、manifest 最后写、原子提交、启动恢复 | M5 |
| NVMe 断电损坏 | 缓存索引丢失 | 版本块格式、校验、扫描恢复和故障注入 | M7 |
| 上位机 v1 明文无鉴权 | 数据窃听、伪造命令、中间人攻击 | ADR-012 风险接受；隔离 VLAN、防火墙/ACL、物理访问控制，未来安全增强使用新协议版本 | M8 |
| 算法无验收数据 | 无法证明有效 | M6-00 设置硬门禁，版本化数据集和指标 | M6 |
| SDK/Qt 绝对路径污染 | 发布包仅开发机可用 | 用户预设、导入目标和干净机 Release 验证 | M0、M3、M9 |
| 7 天测试太晚发现泄漏 | 发布延期 | 从 M2 起加入短时 soak 和资源趋势监测 | M2～M9 |
| 磁盘清理误删未上传事件 | 生产数据丢失 | 默认禁止删除未上传/锁定事件，数据库状态与目录对账 | M5 |

## 19. 后续逐条执行方法

领取任务时使用任务编号，例如“执行 M1-04”。执行者应：

1. 阅读需求文档、本路线图、架构文档和相关源码；
2. 若任务复杂，复制 `.agent/PLANS.md` 中模板创建 `.agent/plans/<task-id>-<slug>.md`；
3. 只实现该编号范围，不自动进入下一个任务；
4. 在 ExecPlan 中持续更新进度、决策、意外发现和验证证据；
5. 完成后回写本路线图任务状态；
6. 最终说明修改文件、验证命令、结果、硬件验证状态和剩余限制。

如果某任务发现必须改变模块边界，应先更新并评审 `docs/architecture/system-architecture.md`；如果改变公开需求，应先更新需求和本追踪矩阵。
