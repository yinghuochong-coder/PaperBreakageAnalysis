## 项目

本仓库实现用于纸机断纸分析系统的边缘工业计算机软件。

目标环境：

* Windows 10/11 x64
* Visual Studio 2026 MSVC
* C++20
* Qt 6
* CMake
* Hikrobot MVS SDK
* 最多支持四台 MV-CS020-60GM 相机

请阅读 `docs/requirements/edge-system-requirements.md`，了解系统需求。

在修改模块边界之前，请阅读 `docs/architecture/system-architecture.md`。

在开始里程碑任务之前，请阅读 `docs/roadmap/development-roadmap.md`。

## 工作规则

* 仅实现当前提示中明确分配的任务。
* 不要自动开始后续里程碑。
* 不要进行与当前任务无关的重构。
* 编辑前，先检查相关源文件和任务文档。
* 对于复杂功能，请按照 `.agent/PLANS.md` 创建或更新 ExecPlan。
* 无法访问硬件或供应商 SDK 时，必须明确说明相关假设。
* 绝不能声称已执行实际并未进行的硬件测试。
* 保持对 Windows 和 MSVC 的兼容性。
* 使用 C++20 和 RAII。
* 避免使用保存全局可变业务状态的单例。
* 应用程序代码中不要直接使用原始 `new` 或 `delete`。
* 不要引入无界队列。
* 不要在相机采集回调中执行磁盘写入、网络操作、JPEG 编码或推理。
* 所有 Hikrobot MVS SDK 调用必须限制在 Hikrobot 适配器模块内部。
* 所有跨线程队列都必须定义容量和溢出策略。
* 所有工作线程都必须具备确定性的关闭路径。
* 不要将供应商错误码作为唯一的业务错误码对外暴露。
* 引入生产环境依赖前，必须记录引入原因。

## 构建

使用已配置的 CMake 预设。

典型命令：

```powershell
cmake --preset windows-vs2026-debug
cmake --build --preset windows-vs2026-debug
ctest --preset windows-vs2026-debug
```

如果这些预设尚不存在，仅在当前分配的任务确实需要时才创建它们。

## 测试

* 对每项行为变更，在实际可行的情况下新增或更新测试。
* 自动化测试应使用模拟相机。
* 除非明确标记为硬件集成测试，否则测试不得依赖实体相机。
* 除非相关构建和测试命令确实已经运行，否则不得报告成功。
* 对无法执行的命令进行说明，并解释原因。

## 完成标准

只有同时满足以下条件，任务才算完成：

* 已满足任务中规定的验收标准；
* 已新增或更新相关测试；
* 已运行相关构建和测试；
* 未修改无关文件；
* 当公开行为或架构发生变化时，已更新相关文档；
* 最终回复中列出了修改的文件、验证命令、执行结果以及仍存在的限制。

## Git 和任务范围

* 除非明确要求，否则不要提交或推送代码。
* 不要修改仓库之外的文件。
* 未经明确授权，不要删除现有数据或配置。
* 每项任务应足够小，以便作为一个逻辑变更进行审查。
